#!/bin/bash

. $WM_PROJECT_DIR/bin/tools/RunFunctions

source globalPars
rm setSet.batch
cat "setSet.template" | while read line; do
  eval echo $line >> setSet.batch
done
runApplication setSet -batch setSet.batch -noVTK
rm constant/polyMesh/sets/*_old